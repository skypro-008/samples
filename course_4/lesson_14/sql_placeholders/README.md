# Использование плейсхолеров в SQL запросах

Есть несколько безопасных способов добавлять параметры в SQL запроса.
Здесь мы рассматриваем передачу параметров в виде словаря:

```
cur.execute(query, {"key": "value")
```
